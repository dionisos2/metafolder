#+TITLE: Metafolder — Query Language Specification
#+OPTIONS: toc:2 num:t

* Overview

The query system allows filtering metadata metarecords by field values, presence,
and graph relationships. Queries are represented as a recursive JSON structure
and compiled to SQL on the daemon side. The CLI provides a human-friendly DSL
that compiles to this representation.

* Concepts

Queries are represented as a recursive JSON structure (the =Query= enum). The
CLI provides a DSL that compiles to this representation.

** Variants

| Variant             | JSON =type= key       | Description                                    |
|---------------------+-----------------------+------------------------------------------------|
| IsPresent           | =is_present=          | Field exists and is not =Nothing=              |
| IsAbsent            | =is_absent=           | Field exists and is =Nothing=                  |
| IsUnknown           | =is_unknown=          | Field does not exist at all                    |
| Eq / Neq            | =eq= / =neq=          | Equality / inequality comparison               |
| Lt / Lte / Gt / Gte | =lt= / =lte= / ...    | Ordered comparison                             |
| And                 | =and=                 | All operands must match                        |
| Or                  | =or=                  | At least one operand must match                |
| Not                 | =not=                 | Operand must not match                         |
| Follows             | =follows=             | =Ref= or =TreeRef= field: direct referent matches condition    |
| FollowsTransitive   | =follows_transitive=  | =TreeRef= only: metarecord is a descendant of the target (a path, or any metarecord matching a condition) |
| Matches             | =matches=             | String field matches a regex pattern                           |
| UuidIn              | =uuid_in=             | Metarecord UUID is in an explicit list (=uuids=, 32-hex)        |

=UuidIn= is the bridge between the resource layer (a single thing at its URL)
and the set layer: it lets the =/query/*= operations target an *explicit set of
UUIDs* instead of a predicate. JSON: ={"type": "uuid_in", "uuids": ["<32hex>", ...]}=
(an empty list matches nothing). It is the canonical way to read or write *several
named metarecords at once* in one request — e.g. a GUI multi-selection — rather
than one request per UUID.

* Daemon

** Compilation

Queries are compiled to a CTE chain in SQL. Each query node becomes one CTE;
the final SELECT is filtered via =metarecord_db= to isolate the repository and
exclude link metarecords by default.

Exception: =FollowsTransitive= on a =TreeRef= field is executed as a hybrid
operation — the daemon first collects the descendant UUID set iteratively via
the tree cache, then injects it as an =IN= clause into the surrounding SQL query
rather than emitting a CTE.

This describes the *SQL engine*, which is the fallback. The common queries —
including =Path=-target =Follows=/=FollowsTransitive= such as
=mfr_path ->* "/dir"= with a sort — are served entirely in memory by the bitmap
index (the path is resolved to a root through the tree cache, the descendants
by bitmap expansion); see [[file:spec-indexing.org][spec-indexing.org]].

=Follows= takes either a condition (sub-query) or a path string as right-hand
side. With a condition, it matches metarecords whose =value_uuid= (the =Ref=
referent, or the =TreeRef= direct parent) points to a metarecord satisfying
that condition; this works on both =Ref= and =TreeRef= fields, e.g.
=mfr_path -> (mfr_path = "2021")= matches the direct children of every
directory named =2021=. With a path string (e.g. ="/music/jazz"=, =TreeRef=
semantics only), the path is resolved to a UUID via the tree cache and the
query matches metarecords whose direct parent (=value_uuid=) is that metarecord.
DSL syntax: =field -> "path"= / =field -> (predicate)=.

=FollowsTransitive= is restricted to =TreeRef= fields; applying it to a =Ref=
field is rejected with =400=. The right-hand side is either a path string
(e.g. ="/projects"=) that is resolved to a UUID via the tree cache, or a
condition (sub-query, executed on its own and repo-filtered) whose matches
form the root set — e.g. =mfr_path ->* (mfr_path = "2021")= matches
everything inside any directory named =2021=. All descendants of the root(s)
are then collected iteratively via the tree cache (fetching missing nodes
from the DB step by step, deduplicating across overlapping roots), and the
resulting UUID set is used to filter the query results. The roots themselves
are not part of the result.

String operations on a =TreeRef= row apply to =value_name= (the single name
component) — the same convention everywhere: comparisons (=Eq=/=Neq= and the
ordered operators) with a string operand, =Matches=, and sorting. So
=mfr_path = "2021"= matches every metarecord whose =mfr_path= name is exactly
=2021=, wherever it sits in the tree. Strict =(parent, name)= equality remains
available through =Eq= with a =tree_ref= JSON value (no DSL literal exists for
it; the same pair can be expressed in the DSL as
=field -> "/parent/path" AND field = "name"=).

=Matches= uses a SQLite user-defined function =REGEXP= (registered at connection
setup) backed by the =regex= crate. On a =TreeRef= field, =MATCHES= applies the
regex to =value_name= (the single name component).

** Comparison typing

A field name has a single non-=Nothing= value type repository-wide (see
[[file:spec-data-model.org][spec-data-model.org]] → "One value type per field
name"), so comparisons are always *same-type*: =Eq=/=Neq= and the ordered
operators compare the operand against rows of the field's one type, with no
cross-type coercion rules to define. =Int= and =Float= remain mutually
comparable as the single numeric domain; =DateTime= compares numerically by its
Unix-ms value. A =Nothing= row never matches a value comparison (use
=IsAbsent=/=IsUnknown= for absence). The =TreeRef= name convention above is the
one apparent exception — a string operand compares against =value_name=, not a
different type.

*** Comparison validity

Two comparison shapes have no well-defined, useful meaning and are rejected with
=400= *upfront*, before any engine runs, so the rejection never depends on which
engine (bitmap index or SQL) would have handled the query:

- A comparison whose operand is =Nothing= (=eq=/=neq= or ordered). Absence is
  expressed with =IsAbsent= / =IsUnknown=, not =eq nothing=.
- An *ordered* comparison (=lt= / =lte= / =gt= / =gte=) on a value type with no
  meaningful order: =Bool= and the reference types (=Ref=, =RefBase=, =TreeRef=,
  =ExternalRef=). Equality (=eq=/=neq=) on these is allowed; ordered comparison
  on =String= (lexicographic), the numeric domain and =DateTime= is allowed.

The validation is a single pure pass over the query IR
(=query_exec::validate_query=); the per-row checks in the SQL engine and the
index's =Unsupported= branches for these shapes are defensive backstops.

** Indexing & scaling

Filtered + sorted + paginated query performance at large scale (the EAV
=field=-scan and conjunction costs, the proposed bitmap/BSI architecture and
adaptive planner) is analysed in its own design document,
[[file:spec-indexing.org][spec-indexing.org]]. The two baseline indexes
described there are implemented; the rest is forward-looking.

** MATCHES acceleration via FTS5 (implemented)

=MATCHES= runs a regex with the =REGEXP= UDF; a full scan of the candidate rows
costs ~650 ms at 50 k. To accelerate it, an FTS5 trigram virtual table indexes
the textual field columns and pre-filters the scan to the rows that can match.

#+BEGIN_SRC sql
CREATE VIRTUAL TABLE field_text USING fts5(
    text, content='', contentless_delete=1, tokenize='trigram'
);
#+END_SRC

One =field_text= row is maintained per =field= row that =MATCHES= scans —
=string= (=value_text=) and =tree_ref= (=value_name=) — with =rowid= equal to
=field.id=. (=datetime= is named in the original design but =MATCHES= does not
scan it, so it is not indexed.) The daemon keeps the index in sync inside the
same transaction as every field write (no triggers; the event log stays the only
side-channel):

- *Inserts* go through the single chokepoint =db::insert_field_row=, which
  *upserts* (delete-by-rowid then insert) the indexed text. This is the only
  maintenance required for *correctness*: because =field.id= is
  =AUTOINCREMENT= (never reused), a =field_text= that is a *superset* of the
  live rows is always correct — a stale rowid points at a deleted =field= row,
  so the =REGEXP= re-check on the live rows excludes it. Missing entries (false
  negatives) are the only error, and the chokepoint prevents them.
- *Deletes* drop the matching =field_text= entries at the =Writer='s delete
  sites (=db::delete_field_text_by_id= / =_by_name= / =_by_metarecord=). This is
  *best-effort* — only to bound the index size; a missed delete (e.g. on the
  rarer log-navigation paths) leaves a harmless stale entry.
- =db::ensure_field_text= builds the index for repositories created before it
  existed, and doubles as a rebuild/compaction entry point.

Query plan for =MATCHES= (=query_exec=):

1. Extract, from the parsed regex HIR (=regex-syntax=, in =fts.rs=), the longest
   literal substring of ≥ 3 characters that *every* match must contain. The
   extraction is *conservative*: alternations, optional/zero-min repetitions and
   character classes break a literal run, and anything uncertain yields *no*
   literal. The soundness obligation (the literal is contained in every match)
   is cross-checked against the full scan by =tests/fts_oracle.rs=.
2. If a literal exists, restrict the scan:
   =… AND id IN (SELECT rowid FROM field_text WHERE text MATCH '"<literal>"')=
   (the literal bound as a quoted phrase), then apply =REGEXP= only to the
   survivors. (A membership test, not a join: driving from the FTS rowids was
   measured slower once wrapped in the repository-isolation CTE.)
3. If no literal can be extracted (e.g. =^.*$=, =cat|dog=, =(?i)…=), fall back to
   the v1 full scan.

The pre-filter is purely an optimisation — the trigram tokenizer is
case-insensitive and the match is a sound over-approximation, so =REGEXP= always
re-checks and results are identical to the full scan. Measured at 50 k: a
selective literal drops from ~650 ms to tens of ms; a literal whose trigrams are
common in the corpus still beats the full scan by ~3×; a no-literal pattern is
unchanged. FTS5 is also the planned acceleration for high-cardinality string
equality if needed.

** HTTP API

*** =POST /repos/:repo_uuid/query=

#+BEGIN_SRC
POST /repos/:repo_uuid/query
#+END_SRC

Executes a query and returns matching metarecords.

Request body:
#+BEGIN_SRC json
{
  "query": <Query>,
  "select": ["path", "rating"],
  "sort": [{"field": "rating", "order": "desc"}],
  "limit": 500,
  "cursor": "..."
}
#+END_SRC

=query= is a =Query= JSON object (see [[*Query Language]]).

=sort=, =limit= and =cursor= are optional and enable sorted/paginated
results; their semantics (including the response envelope used when =limit=
is present) are defined in the Pagination section of
[[file:spec-data-model.org][spec-data-model.org]].

=select= is optional:
- Absent → response is =["uuid1", "uuid2", ...]=
- =["field1", "field2", ...]= → response is a list of metadata objects
  containing only the requested fields.
- ="*"= → response is a list of full metadata objects (all fields).

Example response with =select=:
#+BEGIN_SRC json
[
  {
    "uuid": "...",
    "version": 3,
    "fields": [
      {"id": 42, "name": "path", "value": {"type": "tree_ref", "value": {"parent": "<parent_uuid>", "name": "bar.mp3"}}},
      {"id": 43, "name": "rating", "value": {"type": "int", "value": 5}}
    ]
  }
]
#+END_SRC

Fields absent from the metarecord (not present in the DB for that metarecord) are simply
omitted from the response — they are not included as =Nothing=.

*** The =/query/*= family (operations over a set)

Every operation over a *set* of metarecords lives under =/query=, with a
=query= field in the body (use a =uuid_in= predicate to target an explicit set,
e.g. a multi-selection). This is the only mechanism for set-scoped reads and
writes; single, directly-addressed things use the resource endpoints in
[[file:spec-data-model.org][spec-data-model.org]] (=/metarecords/:uuid=, =/metarecords/:uuid/fields/:name=,
=/fields/:id=). The two overlap only on the n=1 case (direct = cheap/idempotent,
=/query= = one call over a set), by design.

The field writes are each *one log operation per affected metarecord* (see
[[file:spec-data-model.org][spec-data-model.org]] "Log granularity"); a write to a protected field name
without =force= returns =400= ([[*Protected fields]]).

**** =POST /repos/:repo_uuid/query/delete=

Deletes every matching metarecord (metadata and all fields) in one transaction
(one revision, rollback-able as a unit) — atomic, free of the client-side
select-then-delete TOCTOU. =mfr_*= fields are never written.
Body ={"query": <Query>}= → ={"deleted": <count>}=.

**** =POST /repos/:repo_uuid/query/fields/set=

Replaces *all* rows of a field on every match, as one =SetField= op per
metarecord. Body =value= sets one row, =values= (a list) sets several at once
(multi-map) — exactly one of the two:
#+BEGIN_SRC json
{ "query": <Query>, "name": "tag",
  "values": [{"type": "string", "value": "a"}, {"type": "string", "value": "b"}] }
#+END_SRC
Response ={"updated": <count>}=.

**** =POST /repos/:repo_uuid/query/fields/append=

Appends one row to every match (multi-map; never replaces). Body
={"query", "name", "value", "force"?}= → ={"updated": <count>}= (each match
gains one row).

**** =POST /repos/:repo_uuid/query/fields/remove=

Removes every row equal to =(name, value)= from each match — the *inverse of*
append — as one =DeleteField= op per metarecord. Other rows of the same name and
non-matching metarecords are untouched. Body ={"query", "name", "value"}= →
={"updated": <count>}= (metarecords actually changed).

**** =POST /repos/:repo_uuid/query/fields/unset=

Removes the field *entirely* (every row of =name=) from each match, as one
=DeleteField= op per metarecord → the field becomes /unknown/, distinct from
=Nothing=. Body ={"query", "name"}= → ={"updated": <count>}=.

**** =POST /repos/:repo_uuid/query/fields/resolve-tree=

Resolves a =TreeRef= field to its root-relative path(s) for every match — the
set form of the direct =GET …/fields/:name/resolve-tree= ([[file:spec-data-model.org][spec-data-model.org]]).
A read/projection (not a write), but set-scoped, so it lives here. Body
={"query": <Query>, "field": "mfr_path"}= (=field= defaults to =mfr_path=) →
={"<uuid>": ["<path>", ...], ...}= (paths are root-relative, no leading slash; a
metarecord absent from the result carries no resolvable path). The tree-cache
serves it from memory; the GUI memoises it per =(field, uuid)=.

* CLI

** Query DSL

The CLI accepts queries as a human-friendly DSL string, parsed by a
hand-written lexer + recursive-descent parser and compiled to the =Query=
JSON representation.

The parser is shared code: it lives in the =core= crate
(=metafolder_core::dsl::parse_query=) and is used by both the CLI and the
GUI (the =metarecord-list= query editor, see [[file:spec-gui.org][spec-gui.org]]).

A higher-level, user-configurable surface that transpiles to this DSL — the
/simplified query language/ — is specified in [[*Simplified query language]]; it
is the default input of the GUI query editor.

*** Grammar

#+BEGIN_SRC
query     := or_expr
or_expr   := and_expr ( "OR" and_expr )*
and_expr  := unary ( "AND" unary )*
unary     := "NOT" unary | atom
atom      := "(" query ")" | predicate

predicate := field "IS" ( "PRESENT" | "ABSENT" | "UNKNOWN" )
           | field cmp_op literal
           | field "MATCHES" string
           | field "->"  ( string | "(" query ")" )
           | field "->*" ( string | "(" query ")" )

cmp_op    := "=" | "!=" | "<" | "<=" | ">" | ">="
literal   := int | float | string | "true" | "false" | datetime
datetime  := "@" string | "@" int        # ISO-8601 string, or raw Unix ms
field     := identifier
string    := '"' ... '"'
#+END_SRC

- Keywords (=AND=, =OR=, =NOT=, =IS=, =PRESENT=, =ABSENT=, =UNKNOWN=,
  =MATCHES=, =true=, =false=) are case-sensitive uppercase except the boolean
  literals.
- Precedence: =NOT= binds tighter than =AND=, which binds tighter than =OR=;
  parentheses override.
- =field -> "path"= compiles to =Follows= with a path right-hand side
  (=TreeRef= semantics); =field -> (predicate)= compiles to =Follows= with a
  sub-query right-hand side (the referent of a =Ref= row, or the direct
  parent of a =TreeRef= row, must satisfy the predicate).
- =field ->* "path"= / =field ->* (predicate)= compiles to
  =FollowsTransitive= (TreeRef only), with the same two right-hand-side
  forms as =->=.
- A *datetime literal* is =@= followed by either a quoted ISO-8601 string
  (=@"2024-01-01"=, =@"2024-01-01 12:30:00"=; any form =core::date::iso_to_ms=
  accepts, UTC) or a bare integer of Unix milliseconds (=@1704067200000=). It
  compiles to =Value::DateTime= (Unix ms); an unparsable ISO string is rejected.
  This is what makes =mfr_mtime > @"2024-01-01"= compare against =datetime= rows
  (a plain string would not — see [[file:spec-data-model.org][spec-data-model.org]]).

Examples:

#+BEGIN_SRC
rating > 3 AND genre = "jazz"
mfr_path IS ABSENT
NOT (seen = true OR rating IS UNKNOWN)
mfr_path ->* "/music/jazz" AND title MATCHES "[Ll]ive"
author -> (name = "Coltrane")
mfr_type = "file" AND mfr_path -> (mfr_path = "2021")
#+END_SRC

** =mf query <predicate>=

Compiles the predicate and calls =POST /query=. By default prints matching
UUIDs, one per line. The CLI paginates internally (following =next_cursor=)
and streams the output.

| Flag                       | Description                                                          |
|----------------------------+----------------------------------------------------------------------|
| =--select a,b= / =--select '*'= | Print full metadata objects (JSON array) restricted to the listed fields, or all fields with ='*'= |
| =--sort field[:asc\vert{}desc]=  | Sort results; repeatable for secondary keys                          |
| =--limit N=                | Stop after N results                                                 |
| =--values=                 | With =--select <one field>=: print the field's raw values, one per line (multi-map: one line per row; =nothing= rows skipped; scalars bare, refs as 32-hex uuids, structured values as compact JSON) |
| =--simplified= / =-s=      | Treat =<predicate>= as [[*Simplified query language][simplified-language]] text and expand it locally to the normal DSL first (shared grammar in core, no daemon round-trip) before compiling |

* Simplified query language

The normal DSL ([[*Query DSL]]) is precise but verbose for everyday filtering
(=genre = "jazz"=, =AND=, =IS PRESENT=…). The /simplified query language/ is an
ergonomic surface that **transpiles to normal DSL text**, which is then parsed
by =parse_query= as usual. Its entire syntax is defined by a grammar the user
can edit — nothing is hard-coded; the shipped conveniences are default grammar
rules, not built-ins.

** Pipeline

#+BEGIN_SRC
simplified text → [ lexer → grammar engine ] → normal DSL text → parse_query → Query IR
#+END_SRC

The grammar engine lives in the =core= crate: a small hand-written
recursive-descent / PEG-style interpreter with output templates (no external
parser library — runtime-loadable grammars are rare and the template layer is
ours regardless). It emits normal DSL *text*; the existing =parse_query= turns
that text into the =Query= IR. The two stages have distinct error surfaces: a
/grammar error/ (the simplified input does not parse under the grammar) is
separate from a /DSL error/ (the produced text is rejected by =parse_query=).

The simplified language is **not** a superset of the normal DSL: a grammar may
accept neither, either, or a mix. Expansion is therefore **not idempotent** on
normal-DSL input. Consumers keep the two explicit — the GUI editor shows both
(see [[file:spec-gui.org][spec-gui.org]], =metarecord-list= panel), and the CLI gates simplified
input behind =mf query --simplified=.

** Configuration

The grammar is a single file in the shared (core) config directory,
=~/.config/metafolder/core/query-grammar=, installed by
=metafolder-sync-config= (see [[file:spec-config.org][spec-config]]). It is
**global** (not per-repo) and read by each client (GUI/CLI) for client-side
expansion — the daemon never reads it. An empty file = no rules = the
simplified language accepts nothing. A *missing* or *malformed* grammar is an
error when a client loads it (no fall back to an embedded default); the normal
DSL needs no grammar and is unaffected.

** Lexer (fixed)

A fixed tokenizer (not user-configurable) turns the input into a token stream,
discarding whitespace between tokens:

| Token     | Matches                                                                   |
|-----------+---------------------------------------------------------------------------|
| =WORD=    | =[A-Za-z_][A-Za-z0-9_]*=                                                   |
| =NUMBER=  | integer or float literal                                                  |
| =STRING=  | a double-quoted literal ='"' … '"'= (with escapes)                        |
| =SYMBOL=  | a maximal run of punctuation characters (=:=, =~=, =>=, =>==, =..=, =+=, …) |
| =(= =)=   | grouping, each its own token                                              |

Whitespace is insignificant except as a token separator: =genre=jazz= and
=genre = jazz= tokenize identically, and so do =100MB= and =100 MB=. Maximal
munch on =WORD= gives free word boundaries — a literal ="tag"= in the grammar
never matches the token =tags=.

** Grammar notation

A grammar is a list of named productions; the start production is =query=.

| Form                  | Meaning                                                                       |
|-----------------------+-------------------------------------------------------------------------------|
| =name = ...=          | production definition                                                         |
| =a b=                 | sequence                                                                      |
| =a \vert{} b=         | ordered choice — first alternative that succeeds wins, no backtracking afterwards |
| =a*= =a+= =a?=        | repetition: zero+, one+, optional (adjacent)                                  |
| =a ** s= =a ++ s=     | repetition separated by =s=: zero+, one+                                      |
| =( ... )=             | grouping                                                                       |
| ="lit"=               | matches one token whose text equals =lit=                                     |
| =WORD= =NUMBER= =STRING= | matches a token of that class                                              |
| =label:item=          | binds =item='s match to =label= (a single value, or the list for a repetition) |
| =item => template=    | the production's output template                                              |

A production **without** =>= returns the raw source text it matched (so =STRING=
keeps its quotes and =WORD= stays bare).

** Template language

Right of =>=, a template is literal text with two kinds of holes:

- =$name= — interpolate the capture =name= (its text) verbatim.
- ={ expr }= — evaluate =expr= and interpolate the result.

The base type is **text**; every capture is text. There is **no implicit
coercion** — converting text to a number is explicit, which keeps text fidelity
(e.g. ="007"= stays ="007"=) and leaves room for future text/date functions.
Inside ={ }=:

| Construct           | Notes                                                                            |
|---------------------+----------------------------------------------------------------------------------|
| =$name=             | the capture's text                                                               |
| =+ - * / %=         | arithmetic — operands must be numbers                                            |
| =num(x)=            | parse text =x= as a number (error if not numeric)                               |
| =str(x)=            | render =x= as a DSL string literal — idempotent quoting (quote a bare word, leave an already-quoted string) |
| =join(sep, list)=   | join a repetition's captured list with =sep=                                     |
| =now()=             | current time as Unix milliseconds (a number); makes relative-date rules time-dependent, hence expansion of those rules is not reproducible across calls |

Arithmetic is integer unless an operand is a float; the result is reformatted to
text. Text concatenation needs no ={ }= — adjacent literals and =$captures= in
the template already concatenate. The v1 set above is intentionally minimal;
more functions are localized engine additions that do not break existing
grammars (no migration; pre-v0.2).

** Default grammar

Installed and fully editable. Comparison operators mirror the normal DSL (=,
!=, <, <=, >, >=); =field = value= routes by token class so typed values keep
their DSL type (=rating=4= → =rating = 4=, not ="4"=; =seen=true= → =seen = true=).
Conjunction is juxtaposition or the explicit =AND= keyword (=a b= ≡ =a AND b=):

#+BEGIN_SRC
query = q:or                       => $q
or    = items:and ++ ("OR" | "+")  => {join(" OR ", $items)}
and   = items:andterm +            => {join(" AND ", $items)}    # juxtaposition or AND
andterm = "AND" u:unary            => $u
        | u:unary                  => $u
unary = "!" u:unary                => NOT $u
      | a:atom                     => $a
atom  = "(" q:query ")"            => ($q)
      | p:predicate                => $p

# the part edited most of the time: the predicate alternatives
predicate =
    f:field "="  v:STRING   => $f = $v
  | f:field "="  n:NUMBER   => $f = $n
  | f:field "="  "true"     => $f = true
  | f:field "="  "false"    => $f = false
  | f:field "="  w:WORD     => $f = {str($w)}
  | f:field "!=" v:STRING   => $f != $v
  | f:field "!=" n:NUMBER   => $f != $n
  | f:field "!=" "true"     => $f != true
  | f:field "!=" "false"    => $f != false
  | f:field "!=" w:WORD     => $f != {str($w)}
  | f:field "~"  v:value    => $f MATCHES {str($v)}
  | f:field ">=" n:number   => $f >= $n
  | f:field "<=" n:number   => $f <= $n
  | f:field ">"  n:number   => $f > $n
  | f:field "<"  n:number   => $f < $n
  | f:field "?"             => $f IS PRESENT
  | "under" v:value         => mfr_path ->* {str($v)}
  # date macros — see below
  | df:datefield "younger" d:duration            => $df > @{now() - num($d)}
  | df:datefield "older"   d:duration            => $df < @{now() - num($d)}
  | df:datefield "since"   v:value               => $df >= @{str($v)}
  | df:datefield "before"  v:value               => $df < @{str($v)}
  | df:datefield "between" a:value "and" b:value => $df >= @{str($a)} AND $df <= @{str($b)}
  | "tag"                   => tag = "test"      # bare-word alias, kept as a fallback
  | "fav"                   => rating >= 4

# field aliases: map a few names, pass the rest through
field  = "path" => mfr_path
       | "size" => mfr_size
       | w:WORD => $w
value  = STRING | WORD
number = n:NUMBER "MB"   => {num($n) * 1048576}
       | n:NUMBER "mins" => {num($n) * 60}
       | n:NUMBER        => $n

# date fields and durations (durations are milliseconds)
datefield = "modified" => mfr_mtime
          | "created"  => mfr_btime
duration  = n:NUMBER u:dur_unit => {num($n) * num($u)}
dur_unit  = ("s" | "sec" | "secs" | "second" | "seconds") => 1000
          | ("min" | "mins" | "minute" | "minutes")       => 60000
          | ("h" | "hr" | "hrs" | "hour" | "hours")        => 3600000
          | ("d" | "day" | "days")                         => 86400000
          | ("w" | "week" | "weeks")                       => 604800000
#+END_SRC

Ordering matters under ordered choice: the operator forms (=,= =!=,= =~=, =?=…)
precede the bare-word aliases (=tag=, =fav=), so =tag=jazz= and =tag?= still
parse as predicates and only a lone =tag= triggers its alias.

The date macros read as English: =modified younger 3d= (modified within the last
three days) → =mfr_mtime > @<now − 3 days>=; =created older 2 weeks= →
=mfr_btime < @<now − 2 weeks>=; =created since "2024-01-01"= → =mfr_btime >= @"2024-01-01"=;
=modified between "2024-01-01" and "2024-06-01"= →
=mfr_mtime >= @"2024-01-01" AND mfr_mtime <= @"2024-06-01"=. Durations accept the
unit words above and their abbreviations, with or without a space (=30min=, =3d=,
=2 weeks=); absolute dates are quoted ISO-8601. =modified= maps to =mfr_mtime= and
=created= to =mfr_btime=; edit the grammar to target other datetime fields.

** Validation

At load the grammar is checked: every referenced production exists, the start
production =query= is present, **left recursion is rejected** (=or = or ...=
would loop forever) with an error naming the offending production, and every
string literal must tokenize to **exactly one token** under the fixed lexer.
A literal matches one input token by text, so one that lexes to several tokens
(e.g. =\"/fav\"= → =/= , =fav=) or to none (whitespace-only) could never match;
it is rejected with an error naming the pieces and suggesting the split form
(=\"/\" \"fav\"=). Grammar and expansion errors carry the position in the
simplified input.

** Dates

Relative and absolute date filters are supported, on three layers:

1. =datetime= values are stored as Unix milliseconds and compared numerically
   (see [[file:spec-data-model.org][spec-data-model.org]]).
2. The normal DSL has a *datetime literal* =@"<iso>"= / =@<ms>= (see
   [[*Grammar][the DSL grammar]]), so =mfr_mtime > @"2024-01-01"= compiles to a real
   =datetime= comparison.
3. The template language exposes =now()= (current Unix ms), and the default
   grammar ships the =modified=/=created= date macros (=younger=/=older=/=since=/
   =before=/=between=, see [[*Default grammar]]). Relative forms expand to
   =@{now() − duration}=.

Because =now()= is read at expansion time, expanding a relative-date macro is
time-dependent (not reproducible across calls). Calendar-aware durations
(months, years) are intentionally left out; the supplied units run from seconds
to weeks. Richer relative grammars are a matter of editing the grammar file.

** Where expansion runs

Expansion is a **pure, client-side transformation**: it depends only on the
grammar file and the input text, never on the daemon or any repository. The
GUI and CLI link =metafolder-core= and call the shared engine directly
(`core::simplified::engine::expand`) — there is *no* daemon endpoint for it.
The GUI loads the grammar at startup and exposes a local backend command
(=metafolder.query.expand=, see spec-gui); the CLI loads it when
=--simplified= is used. Relative-date macros (=now()=) therefore resolve
against the **client's** local clock.

A grammar/expansion error is reported by the client (the GUI shows it under
the simplified input; a subsequent normal-DSL parse failure is a separate
error shown under the normal-DSL input).

* Open questions
