#+TITLE: Metafolder — Data Model Specification
#+OPTIONS: toc:2 num:t

* Overview

The data model defines the core structures shared across all components:
repositories, metadata metarecords, fields, and values.

* Concepts

** Repository

Persisted as =config.json= inside the =.metafolder/= directory.

By default, =.metafolder/= is a subdirectory of the watched root. It can also
live at an arbitrary path (external database), useful when the data disk is
read-only or when metadata should be kept on a separate, faster device.

The database and other daemon-managed volatile files live in the
=.metafolder/internal/= subdirectory, which is always excluded from tracking
(see [[file:spec-file-tracking.org][spec-file-tracking.org]]). The rest of
=.metafolder/= (=config.json=, =schema.json=) is ordinary content that the
user may track like any other file. Repositories created before the
=internal/= layout are migrated automatically at load time (the =db.sqlite*=
family is moved into =internal/=).

| Field       | Type   | Description                            |
|-------------+--------+----------------------------------------|
| =repo_uuid= | UUID   | Stable identifier for this repository  |
| =name=      | string | Name of the repository                 |
| =version=   | string | Config schema version                  |
| =root=      | path   | Absolute path to the watched directory |
| =schema=    | path   | Optional path to the user schema file, relative to =.metafolder/= (or absolute); default =.metafolder/schema.json= (see [[file:spec-schema.org][spec-schema.org]]) |
| =log_retention_revisions= | u64 | Optional: revisions of history the event log keeps behind HEAD, overriding the daemon's =[settings]= (default 200); =0= keeps everything (see [[file:spec-event-log.org][spec-event-log.org]] "Automatic retention") |
| =log_retention_keep_labels= | bool | Optional: whether a labelled revision stops the trim, overriding the daemon's =[settings]= |

** Metarecord

A metarecord is a coherent set of metadata about one subject — typically a file —
or a value shared by such metadata (a tag, a collection, a cross-repo link).
The fields of a metarecord are its metadata; "metarecord" names the unit, "metadata"
names what the fields are.

| Field     | Type       | Description                                              |
|-----------+------------+----------------------------------------------------------|
| =uuid=    | UUID       | Stable identifier for this metarecord                         |
| =version= | u64        | Content hash of the metarecord's fields, daemon-computed |
| =fields=  | list       | Ordered list of =Field= values                           |

Fields form a *multi-map*: multiple fields with the same name are allowed.

Ownership is implicit: every metarecord belongs to the repository whose
database holds it (one database per repository). Cross-repo sync state (links
between metarecords of two repositories) lives outside the data model, in a
dedicated sync database — see [[file:spec-sync.org][spec-sync.org]].

*** Version

A metarecord's =version= is a *hash of its own content*: the multiset of its
=(name, type, value)= field triples, plus its =uuid=. It is not a counter, and
it carries no ordering — two versions are only ever compared for *equality*.

That equality is the whole point, in both directions:

- equal versions ⇒ identical field states (so a cached copy is still valid);
- identical field states ⇒ equal versions (so a value changed and changed back
  is correctly reported as unchanged).

Both hold *by construction*, across the whole history of a metarecord and across
every branch of the log, with nothing to maintain and nothing to restore. This
is what cross-repo sync's change detection rests on
([[file:spec-sync.org][spec-sync.org]]) and what makes the version a reliable
cache key.

**** What the hash covers

The =(name, type, value)= triple is hashed *as one unit*, so moving a value from
one field name to another, or retyping it, changes the version.

- The multiset is hashed *order-independently*: field rows have no meaningful
  order, and two metarecords carrying the same rows must hash alike.
- =Value::Nothing= is content like any other value: a row holding it contributes
  to the hash, while an absent field contributes nothing. The two states are
  distinct in the data model and must stay distinct here.
- =field.id= is *excluded*. A row id is storage identity, not content; including
  it would re-couple the version to the physical rows and defeat the invariant
  above (navigation restores ids exactly, so the version must not depend on
  them).
- The metarecord's =uuid= is *included*, as a base term, so that two metarecords
  carrying identical fields still hash differently and each record's version
  values live in their own domain. A metarecord with no fields therefore has a
  version of its own rather than a shared zero.
- Repository ownership (=metarecord_db=) is *excluded*: it is not part of the
  metadata the version describes.

**** Incremental maintenance

Recomputing the hash from every row on every write would make a bulk write
(reconcile, a watcher flush) re-read each record it touches. The hash is
therefore *additive*: the version is the sum, modulo 2⁶⁴, of the per-row hashes
and the uuid term. A write subtracts the hash of each row it removes and adds
the hash of each row it inserts — cost proportional to the rows actually
touched, not to the size of the metarecord.

Addition, not XOR. Two equal terms cancel under XOR, so a metarecord holding the
same row twice would hash exactly like one holding it never — and
[[*No duplicate rows]], which forbids that state, is held by the write path
alone: there is deliberately no unique index over the value columns to enforce
it. A hash whose correctness depends on an invariant nothing checks is a hash
that fails silently, and it would fail in the worst possible direction: sync
would read "unchanged". Summation degrades gracefully instead — a duplicated row
simply makes the record hash differently.

The 64-bit width is sized for the comparison that is actually made. Versions are
only ever compared *within one metarecord's own history* (against a sync
baseline, or an =expected_version= precondition), never across a repository, so
a collision would require two states of the *same* record to hash alike.

**** Migration

An existing repository drops =next_version=, recomputes every metarecord's
=version= from its own rows, and *empties the event log* — the =revision=,
=operation= and =op_snapshot= rows, with =log_head.op_id= set back to =NULL=.
Buffered watcher events (=pending_operation=) are not log rows and are kept.

The log is discarded rather than converted because it cannot be converted
cheaply *and* safely: its =entity_version_*= columns hold allocator counters, so
a navigation step restoring one would leave the metarecord describing itself
with a number that is not the hash of the state just restored — the first
rollback would desynchronise every record it touched, and sync would read the
result as a phantom change. The historical hashes could in principle be
recomputed from =op_snapshot=, but that buys a rewind capability no repository
needs at this stage of the project. The repository's *data* is untouched; only
the ability to rewind it is. Trash entries recorded before the migration keep
their blobs and restore normally, but lose their rollback auto-restore
correlation, which is a recorded version.

This bumps =API_VERSION=. The wire shape does not change — =version= is still an
integer, and =expected_version= still takes one — but its meaning does: a client
that ordered two versions would now be comparing hashes.

** Field and Value

A field is a =(name, value)= pair. Value is one of:

| Variant    | Rust type       | Semantics                                    |
|------------+-----------------+----------------------------------------------|
| =Nothing=  | —               | Explicit absence ("this field does not apply") |
| =String=   | =String=        | UTF-8 text                                   |
| =Int=      | =i64=           | 64-bit signed integer                        |
| =Float=    | =f64=           | 64-bit float                                 |
| =Bool=     | =bool=          |                                              |
| =DateTime= | =i64=           | Unix milliseconds (UTC); JSON wire form is an ISO-8601 string (=YYYY-MM-DDTHH:MM:SSZ=) |
| =Ref=          | =UUID=         | Reference to another metarecord's UUID (same repo)              |
| =TreeRef=      | =(UUID?, TreeName)= | Position in a named tree: =(parent_uuid, name)= (see [[*Tree names]]) |
| =RefBase=      | =UUID=         | Reference to a repository UUID                             |
| =ExternalRef=  | =(UUID, UUID)= | Cross-repo reference: (repo UUID, metarecord UUID)              |

A =TreeRef= stores the position of a metarecord in a named tree as =(parent_uuid, name)=,
kept in the =value_uuid= and =value_name= columns of the =field= table
(see [[*SQLite Schema]]):

- =value_uuid=: UUID of the parent metarecord (zero UUID =x'00...00'= for root nodes).
- =value_name=: the name component as *displayed* (e.g. filename, tag name) — what
  the string predicates of [[file:spec-query.org][spec-query.org]] compare against.
- =value_name_bytes=: that name's *exact bytes*, which identify the node and
  carry the forest's uniqueness (see [[*Tree names]]).

Multiple independent trees can coexist within the same repository by using different
field names (e.g. =mfr_path= for the filesystem tree, =parent= for a tag hierarchy).

*** Referential integrity of a forest

A =TreeRef= names its parent by UUID, and the daemon holds *both* ends of that
reference on every manual write:

- **Creating** one: the parent must be a metarecord that itself carries a
  =TreeRef= row *for the same field name*. A parent that does not exist, or that
  exists in another forest only, is rejected with =400= — together with the
  cycle and depth checks (spec-main "Key invariants").
- **Removing** one: a metarecord's last =TreeRef= position for a field name may
  not be removed while other metarecords are placed under it. The write is
  rejected with =400= naming the children. This covers every manual shape that
  can take a position away: =unset=, deleting the field row, setting the field
  to =Nothing= or to something else, a whole-record overwrite, and deleting the
  metarecord.

The second check runs **once, against the state the revision is about to
commit**, not per operation: deleting a whole subtree in one revision is
legitimate even though the parent goes first, and only the committed state has
to be a forest.

*Deliberately neither cascade nor silence.* Cascading — "removing a directory
removes everything under it" — is what the *watcher* does for =mfr_path=, where
the filesystem is authoritative and a vanished directory really is gone
(see [[file:spec-file-tracking.org][spec-file-tracking.org]], and the
=orphan-cascade-limit= circuit breaker that guards it). A manual write has no
such authority, so it is refused rather than allowed to erase a subtree the
caller did not name. Watcher-originated operations (=file_deleted=, =file_moved=,
=file_modified=) are therefore exempt, and so is log navigation, which restores
rows directly and moves through intermediate states on its way to a state that
was valid when it was written.

Until this check existed, taking a parent's position away left its children
naming a node that no longer existed — *detached*: reachable by uuid, but under
no parent and in no roots map, so =GET /tree/roots= and path reconstruction
disagreed about them ever after. Repositories written before it may still hold
one; the index and the oracle agree about them (=tests/index_oracle.rs=), and
this lists them:

#+BEGIN_SRC sql
SELECT c.field_name, hex(c.metarecord_uuid), c.value_name, hex(c.value_uuid)
FROM field c INDEXED BY idx_field_tree
WHERE c.value_type = 'tree_ref'
  AND c.value_uuid != x'00000000000000000000000000000000'
  AND NOT EXISTS (SELECT 1 FROM field p INDEXED BY idx_field_metarecord
                  WHERE p.metarecord_uuid = c.value_uuid AND p.field_name = c.field_name
                    AND p.value_type = 'tree_ref');
#+END_SRC

*** Tree names

A POSIX file name is a *byte string* — any sequence but =/= and NUL — and is not
required to be UTF-8. Legacy collections are full of latin-1 names, and such a
file must be trackable like any other, so a name cannot simply be a string.

A =TreeName= therefore carries the **exact bytes**, and the text is derived from
them:

- the **bytes are the identity**: equality, ordering, hashing and the forest's
  uniqueness constraint all go through them, so two files whose names differ
  only in undecodable bytes are two distinct nodes;
- the **text is presentation only**, and it is what a query spells. A name that
  *is* text shows exactly as it is — =100%.txt= stays =100%.txt=, so the common
  case pays nothing and no stored name ever had to be rewritten. Only a byte no
  text can represent becomes =%XX=, its value in hex: =caf\xE9.mp4= shows as
  =caf%E9.mp4=. One escape per faulty **byte** — an invalid byte is the code of
  no character, so there is nothing else to write.

This split is forced, not a preference. An encoding that both leaves every valid
UTF-8 name unchanged *and* is injective cannot exist: if all valid text maps to
itself, the text form is entirely used up and no undecodable name can map
anywhere free. Identity must therefore live in the bytes. The escaping above is
consequently *not* a bijection — a file really named =caf%E9.mp4= shows the same
text — which is exactly why a lookup searches both readings (below) rather than
picking one.

Every path the daemon handles carries these bytes end to end: the reconcile
walk, the watcher (whose buffered events persist the exact path alongside its
displayed form, so a restart loses nothing), the executor, and
=POST /repos/:repo/track=. Where *text* is meant, text is used: =mf_ignore=
patterns are regexes the user wrote against displayed paths, and API fields
report the displayed form.

*Looking a name up.* A typed component has up to two byte readings, and **both
are searched, the results added**:

- *verbatim* — the bytes of what was typed. This is what finds a file whose name
  really contains those characters, and it is the only reading for text that is
  not our escaped form: no escape at all, a malformed one (=100%.txt=, =%zz=,
  =%E=), an escape below =0x80= (such a byte is always valid UTF-8, so it is
  never produced — refusing it keeps =50%2E.txt= from also being searched as
  =50..txt=), or U+FFFD, which is a lossy rendering pasted from another tool and
  cannot be decoded back (the replacement character is three bytes; the byte it
  stood for is gone).
- *escaped* — the bytes that form decodes to, when it is one.

Adding them is deliberate: neither reading is wrong, and a user hunting for a
file named =%1234.txt= types exactly that. A query returns a set, so returning
both is honest. The two are told apart by the *marker* on the answer: a name
that needed escaping travels in the object wire form below, so a client can show
it as such.

A **resolution** that must yield a single metarecord (=resolve-tree=, =mf path=,
GUI navigation) cannot add them. It takes an optional =form= parameter
(=verbatim= or =escaped=) selecting one reading, which is then never ambiguous;
without it, two matches are reported as an ambiguity rather than guessed.

*Wire form.* A name that is valid UTF-8 (the overwhelmingly common case) is a
plain JSON string, byte-for-byte the shape every earlier client read. Only a
name that no text can represent takes the object form, carrying both what to
show and what identifies it:

#+BEGIN_SRC json
{"type": "tree_ref", "value": {"parent": null, "name": "vidéo.mp4"}}
{"type": "tree_ref", "value": {"parent": null,
                               "name": {"text": "caf\ufffd.mp4",
                                        "bytes": "636166e92e6d7034"}}}
#+END_SRC

=bytes= (lowercase hex) is authoritative; =text= is carried for readers that
only display. A client that assumed a string meets a different shape rather than
a silently wrong name — which is why this bumps =API_VERSION=.

*Migration.* An existing repository gains =value_name_bytes= (on =field= and on
every table that snapshots a value: =op_snapshot=, so a rollback restores the
exact name, plus =pending_operation= and =snapshot_field=) the next time it is
opened, and the forest's unique index is re-keyed onto it. *No name is
rewritten*: every name such a database holds is valid UTF-8 — an undecodable one
could not have been stored at all — so =CAST(value_name AS BLOB)= derives the
bytes losslessly. The back-fill runs only when the column is being added, so
re-opening never re-derives bytes from the (now possibly lossy) text.

Rename (same parent, new name): update =value_name= of one row — O(1).
Move (different parent): update =value_uuid= of one row — O(1).

Path-to-UUID resolution is accelerated by the *Tree Cache* (see [[file:spec-file-tracking.org][spec-file-tracking.org]] "Tree Cache").

=Nothing= is distinct from the field simply not existing:
- field present with =Nothing= value → "this field does not apply" (IsAbsent)
- field absent entirely → "unknown" (IsUnknown)

All values serialize to JSON as ={"type": "<variant>", "value": <json_value>}=.
=DateTime= is held internally as Unix milliseconds but encoded on the wire as an
ISO-8601 UTC string; a non-parsable datetime string is rejected on deserialization.

** One value type per field name

A field name has a *single non-=Nothing= value type* across the whole
repository. =Nothing= always coexists with it (it is absence, not a type): a
field may be its type where it applies and =Nothing= where it does not. The
multi-map is unaffected — several rows of the same name are still allowed, but
they all share that one type (e.g. many =tag= rows, all =String=; many =author=
rows, all =Ref=). Mixing value types under one field name is rejected.

The reference variants are treated as *distinct* types, not a family: a field is
=Ref=, or =RefBase=, or =ExternalRef=, or =TreeRef= — never a mix. A link that
is sometimes local and sometimes cross-repo is modelled uniformly as
=ExternalRef=.

The type is established by the first non-=Nothing= write to that name; the user
schema may also declare it explicitly (see
[[file:spec-schema.org][spec-schema.org]]). A later write of a conflicting type
is rejected with =400=. The current implementation realises the =field_type=
registry as a *derived probe* with no separate registry table: the established
type is read as one representative non-=Nothing= row of the name
(=… WHERE field_name = ? AND value_type != 'nothing' LIMIT 1=, seeking the
=field_name= range via the existing =idx_field_name=, stopping at the first such
row), amortised by a per-revision cache in the =Writer= (one probe per field
name per revision). The type therefore unlocks automatically when a name's last
non-=Nothing= value is removed. A materialised registry (and its role as the
per-field index-encoding selector) comes with the indexing work (see
[[file:spec-indexing.org][spec-indexing.org]]).

This uniformity simplifies the rest of the model: comparisons
([[file:spec-query.org][spec-query.org]]) are same-type, and sorting needs no
cross-type precedence (see [[*Sort specification (=POST /query=)]]).

*** Changing a field's type (retype)

Because the first write locks the type, an explicit *retype* operation changes a
field name's type repository-wide, converting every existing non-=Nothing= row.
The target may be *any* value type (=string|int|float|bool|datetime|ref|tree_ref|
externalref|refbase=): a user who mis-typed a field must never be stuck — every
type is reachable from every other.

- HTTP: =POST /repos/:repo_uuid/retype= with body ={"name": "<field>", "to":
  "<type>"}=; CLI: =mf retype <name> <type>=.
- It is a logged write (one revision through =log::Writer=, so it is rollbackable
  like any other change) and updates the =field_type= registry.
- =String= is the *universal hub*: the conversion is lossless and reversible in
  both directions for every type.
  - *any type → =String=* uses its canonical display form: scalars as text,
    =Ref=/=RefBase= as a 32-char hex uuid, =TreeRef= as =<parent_hex>/<name>= (or
    =/<name>= for a root), =ExternalRef= as =<repo_hex>:<metarecord_hex>=.
  - *=String= → any type* parses that same form: =Int/Float/Bool/DateTime= as
    today, =Ref/RefBase= from a hex uuid, =TreeRef= from the =<parent>/<name>=
    grammar, =ExternalRef= from =<repo>:<metarecord>=.
- Other lossless scalar conversions are kept (=Int ↔ Float=, =Bool ↔ Int= as
  =0/1=, =DateTime ↔ Int= as Unix-ms). Conversions between two *different*
  reference types, or from a reference type to a non-=String= scalar (and vice
  versa), have no meaningful coercion and fall back (route the value through
  =String= explicitly if a reinterpretation is intended).
- A value that cannot be converted is set to the target type's *default
  sentinel*, chosen to be easily found afterwards: =Int= =0=, =Float= =0.0=,
  =Bool= =false=, =String= ="="" (empty), =DateTime= the Unix epoch
  (=1970-01-01T00:00:00Z=), the reference types =Nothing= (findable via
  =IS ABSENT=). The operation returns a summary (counts, and the uuids whose
  values fell back to the sentinel) so they can be located and fixed.
- A =String → TreeRef= result is validated against the forest like any TreeRef
  write (parent must exist, no cycle, depth ≤ 1000); a value that would violate
  it falls back to =Nothing= (recorded in the summary) rather than aborting the
  whole retype, so one bad path never blocks the rest.
- A converted value that becomes equal to another row of the same name on the
  same metarecord *drops* that row instead of duplicating it (see [[*No duplicate
  rows]]): two values that the conversion made one are one row afterwards.

** No duplicate rows

A metarecord never holds two field rows with the *same name and the same value*.
The multi-map is a *set* of =(name, value)= pairs, not a bag: several =tag= rows
are allowed because their values differ, but the same tag twice says nothing the
first row does not already say.

Every write enforces this by *collapsing*, never by failing:

- *append* (=POST …/metarecords/:uuid/fields=, =POST /query/fields/add=) of a
  value the metarecord already carries under that name is a *no-op*: no row, no
  =version= bump, no logged operation. The metarecord is returned unchanged, and
  the =updated= count of the set form counts only the metarecords that actually
  gained a row.
- *set* (=PUT …/fields/:name= with =values=, =POST /query/fields/set=) and the
  *whole-record* writes (=POST /metarecords=, =PUT …/metarecords/:uuid=) keep the
  first occurrence of each value and drop the later ones, in the order given.
- a *retype* whose conversion makes a row equal to a sibling row of the same name
  drops that row (the two values became one).

The single exception is the *by-id* edit (=PATCH /repos/:repo_uuid/fields/:id=,
=mf field set <id>=): it names one specific row, so turning it into the twin of a
sibling is rejected with =400= instead of silently deleting the row the client
just addressed.

=Nothing= is a value like any other here: a field holds at most one =Nothing=
row. The unknown/absent distinction is untouched.

*Log navigation is exempt*: rollback and revert restore the exact rows a snapshot
recorded, ids included, so a revision written before this rule is returned to
faithfully (and a duplicate it recorded comes back with it).

Repositories written before this rule are deduplicated *once*, at their first
open under it: the lowest row id of each =(metarecord, name, value)= group is
kept, outside the event log (a migration is not a revision). The pass records
itself in =migration_state= so no later open re-scans the =field= table
(see [[*SQLite Schema]]).

** Reserved fields

Two categories of reserved field names exist, distinguished by prefix:

- =mfr_*= (*metafolder restricted*) — written by the daemon; require =force= to
  override via the API (see [[*Reserved fields]]).
- =mf_*= (*metafolder*) — read by the daemon to control its behaviour; written
  freely by the user without =force=.

The stored fields for a file/directory metarecord are:

| Field              | Prefix | Value type | Description                                                                                                |
|--------------------+--------+------------+------------------------------------------------------------------------------------------------------------|
| =mfr_path=         | mfr    | TreeRef    | Position in filesystem tree; =Nothing= if explicitly deleted (IsAbsent); stale TreeRef if location unconfirmed; absent if never assigned (IsUnknown) |
| =mfr_type=         | mfr    | String     | ="file"=, ="dir"= or ="symlink"= (symlinks are never dereferenced — see [[file:spec-platform.org][spec-platform.org]]) |
| =mfr_size=         | mfr    | Int        | Size in bytes at last watcher/reconcile update; for a symlink, the length of its own target path (=lstat=) |
| =mfr_mtime=        | mfr    | DateTime   | Last content modification time (from =lstat=; the link's own, for a symlink)                               |
| =mfr_symlink_target= | mfr  | String     | Target **path** of a symlink (from =readlink=); present only when =mfr_type = "symlink"=                   |
| =mfr_btime=        | mfr    | DateTime   | File creation time, when the platform/filesystem provides one; absent otherwise (see [[file:spec-platform.org][spec-platform.org]]) |
| =mfr_permissions=  | mfr    | String     | Unix permission bits as octal string, e.g. ="0644"=; Unix only, never written on Windows (see [[file:spec-platform.org][spec-platform.org]]) |
| =mfr_uid=          | mfr    | Int        | Owner user ID; Unix only, never written on Windows                                                         |
| =mfr_gid=          | mfr    | Int        | Owner group ID; Unix only, never written on Windows                                                        |
| =mfr_inode=        | mfr    | String     | ="<dev>:<ino>"=; present only on a file whose link count exceeds one — its presence is what marks a *hard-linked* file (see [[file:spec-duplicates.org][spec-duplicates.org]]). Unix only, never written on Windows |
| =mfr_mime=         | mfr    | String     | MIME type from content analysis (see [[file:spec-platform.org][spec-platform.org]]); computed by reconcile (opt-out) for files that lack it; absent when undetectable or not yet computed |
| =mfr_partial_hash= | mfr    | String     | Hex xxHash3 of first+last 4 KB; absent if not yet computed or invalidated by =Modify=                      |
| =mfr_full_hash=    | mfr    | String     | Hex xxHash3 of full content; absent if not yet computed                                                    |
| =mfr_hash_mtime=   | mfr    | DateTime   | The file's =mfr_mtime= when the stored hashes were computed; written and cleared with them (see [[file:spec-duplicates.org][spec-duplicates.org]] "The hash cache") |
| =mfr_hash_size=    | mfr    | Int        | The file's size when the stored hashes were computed; written and cleared with them |
| =mfr_mount=        | mfr    | String     | Present on a *directory* metarecord that is a mount point: best-effort identity of the mounted volume (see [[file:spec-file-tracking.org][spec-file-tracking.org]] "Mount points"). Its presence, not its value, marks the mount point; never cleared by the daemon |
| =mfr_duplicate_group= | mfr | Ref        | The =duplicate_group= metarecord this file belongs to; present ⟹ the file exists and had a byte-identical twin at the last scan (see [[file:spec-duplicates.org][spec-duplicates.org]]) |
| =mf_watch=         | mf     | Bool       | Opt-in watch flag; controls whether this metarecord and its descendants are tracked (see [[file:spec-file-tracking.org][spec-file-tracking.org]] "Watch and Ignore")  |
| =mf_ignore=        | mf     | String     | Regex pattern (multi-map: one row per pattern); controls which paths are excluded (see [[file:spec-file-tracking.org][spec-file-tracking.org]] "Watch and Ignore") |
| =mf_sync=          | mf     | String     | =internal= (default) / =external=: whether metafolder or an external tool owns this metarecord's content during cross-repo sync; inherited down the =mfr_path= tree (see [[file:spec-sync.org][spec-sync.org]]) |

The user schema feature defines one additional reserved =mf_*= field:
=mf_schema= (String, multi-map — metarecord type declaration). The constraints
themselves live in an external schema file, not in the database; see
[[file:spec-schema.org][spec-schema.org]].

Duplicate detection adds one *daemon-created* metarecord type that is not a file:
a =duplicate_group= (=mf_schema = "duplicate_group"=, no =mfr_path=), carrying
=mfr_content_hash=, =mfr_content_size=, =mfr_duplicate_count= and
=mfr_duplicate_reclaimable=. It is the only place where the daemon writes an
=mf_*= field of its own accord; no =force= is involved, since =mf_*= fields need
none. See [[file:spec-duplicates.org][spec-duplicates.org]].

The =mfr_path= TreeRef stores =(value_uuid, value_name)= for each metarecord.

The three states of the =mfr_path= field have distinct semantics:
- =mfr_path= present (valid TreeRef) → the metarecord is at that location.
- =mfr_path= stale (TreeRef whose path no longer exists on disk) → location
  uncertain; reconcile will attempt to find a match via fingerprint or
  similarity and return it as a candidate.
- =mfr_path= =Nothing= (IsAbsent) → file was explicitly removed (watcher
  =Remove= / =Rename(From)= event); no search needed. *Only the watcher sets
  =mfr_path= to =Nothing=; reconcile never does.*
- =mfr_path= absent (IsUnknown) → no path was ever assigned.

*=mfr_*= fields* are written by the daemon (watcher and reconcile). Attempting
to modify them without =force= returns =400 Bad Request=:

#+BEGIN_SRC json
{"name": "mfr_permissions", "value": {"type": "string", "value": "0755"}, "force": true}
#+END_SRC

*Warning*: daemon operations always overwrite =mfr_*= fields unconditionally,
regardless of whether the value was previously set with =force=. =force= is an
explicit acknowledgement that the value may be overwritten at any time.

Typical legitimate use-cases: restoring permissions after an erroneous
recursive permission change; correcting a misdetected MIME type; resolving an
=mfr_path= conflict the watcher cannot handle automatically.

*=mf_*= fields* are read by the daemon to control its behaviour and written
freely by the user (no =force= required). They are still reserved: any unknown
=mf_*= field name is rejected with =400= to prevent typos from silently having
no effect.

Direct lookup by path uses the tree cache (see [[file:spec-file-tracking.org][spec-file-tracking.org]] "Tree Cache"); on cache
miss, the daemon resolves each component iteratively via the
=(field_name, value_uuid, value_name_bytes)= index.

Inherited property lookup (e.g. nearest ancestor with =mf_sync=) is performed
in two steps: first, walk the parent chain via the tree cache (fetching missing
nodes from the DB step by step) to collect the ordered list of ancestor UUIDs;
then issue a single query =WHERE metarecord_uuid IN (...)= to retrieve the desired
field across all ancestors at once, keeping the closest result.

* Daemon

** SQLite Schema

Each repository uses its own =.metafolder/internal/db.sqlite= file (WAL mode, foreign
keys enabled). Exception: when a repository is loaded from a network
filesystem (e.g. SSHFS, NFS, SMB), the daemon switches to =DELETE= journal
mode (=PRAGMA journal_mode=DELETE=) at connection time, because WAL requires
shared-memory files (=.wal-shm=) which network filesystems do not support
(see [[file:spec-platform.org][spec-platform.org]]). This switch is applied
per-connection and does not modify the database permanently — the next local
connection resumes WAL mode normally. A benchmark should be run once sync is
implemented to verify that DELETE journal mode is acceptable for large syncs.

*** UUID encoding

All UUIDs (metarecord UUIDs and repository UUIDs) are stored as =BLOB= (16 bytes,
raw binary). This applies uniformly to every UUID column in the schema.

The HTTP API encodes UUIDs as 32-char lowercase hex strings (no hyphens) for
readability and interoperability.

Rationale: 16-byte BLOBs enable fast binary comparison in SQLite indexes and
are the most compact representation. Hex encoding in the API is standard and
unambiguous.

The zero UUID (=x'00000000000000000000000000000000'=) is reserved as a sentinel
for =TreeRef= root nodes and must never be assigned to a real metarecord. UUID v4
has 122 random bits (2¹²² ≈ 5×10³⁶ possible values); the probability of
collision among 1 billion metarecords is ≈ 10⁻¹⁹ — negligible in practice.

#+BEGIN_SRC sql
CREATE TABLE metarecord (
    uuid          BLOB    PRIMARY KEY NOT NULL,  -- 16-byte UUID
    version       INTEGER NOT NULL DEFAULT 0     -- content hash of the fields (see "Version")
);

-- The version is a hash of the metarecord's own content, materialized here so
-- that a read never recomputes it and maintained incrementally by every write
-- (see "Version"). It is stored as the i64 bit pattern of the u64 hash.
-- Equal versions imply identical field states and identical field states imply
-- equal versions, by construction — there is no allocator to keep monotonic and
-- nothing for navigation to restore. Cross-repo sync's change detection relies
-- on this ([[file:spec-sync.org][spec-sync.org]]).

CREATE TABLE field (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    metarecord_uuid   BLOB    NOT NULL REFERENCES metarecord(uuid) ON DELETE CASCADE,
    field_name      TEXT    NOT NULL,
    value_type      TEXT    NOT NULL,  -- nothing | string | int | float | bool |
                                       -- datetime | ref |
                                       -- tree_ref | refbase | externalref
    value_text      TEXT,              -- string
    value_int       INTEGER,           -- int, bool (0/1), datetime (Unix ms)
    value_real      REAL,              -- float
    value_uuid      BLOB,              -- ref/externalref: metarecord UUID; tree_ref: parent UUID (zero UUID for root)
    value_ref_repo  BLOB,              -- externalref only: repo UUID
    value_name      TEXT,              -- tree_ref: name component, as displayed
    value_name_bytes BLOB              -- tree_ref: that name's exact bytes (identity)
);

CREATE INDEX idx_field_metarecord        ON field(metarecord_uuid, field_name);
CREATE INDEX idx_field_reverse      ON field(field_name, value_uuid, value_ref_repo)
    WHERE value_type IN ('ref', 'externalref');
CREATE UNIQUE INDEX idx_field_tree  ON field(field_name, value_uuid, value_name_bytes)
    WHERE value_type = 'tree_ref';

-- One-shot migrations that cannot be made into a cheap probe record themselves
-- here, so that opening a repository keeps doing no schema work
-- (see [[*No duplicate rows]]).
CREATE TABLE migration_state (
    name     TEXT PRIMARY KEY NOT NULL,
    done_at  TEXT NOT NULL                -- ISO-8601 UTC
);
#+END_SRC

There is deliberately *no* unique index over the value columns to enforce
[[*No duplicate rows]]: it would index every field row a second time, by its whole
value, roughly doubling the database — for an invariant the write path already
holds (it reads the rows of =(metarecord, name)= before writing them anyway).

** Pagination

=GET /metarecords= and =POST /query= (see
[[file:spec-query.org][spec-query.org]]) support keyset (cursor-based)
pagination. Pagination is opt-in: without a =limit= parameter, the full
result set is returned in one response with the unwrapped response shape, and
nothing changes for existing clients.

*** Request parameters

| Parameter | Type   | Description                                                          |
|-----------+--------+----------------------------------------------------------------------|
| =limit=   | int    | Maximum number of results in this page; enables pagination           |
| =cursor=  | string | Opaque token from the previous page's =next_cursor=; absent for page 1 |
| =sort=    | list   | Sort specification (=POST /query= only; see below)                   |
| =count=   | bool   | Adds =total= (the full result count) to the envelope (=POST /query= only; requires =limit= — the unwrapped response has nowhere to carry it). Computed daemon-side with a =COUNT(*)= over the compiled query, without fetching rows. |

For =GET /metarecords= the parameters are query-string parameters
(=?limit=500&cursor=...=); results are sorted by metarecord UUID (ascending byte
order). For =POST /query= they are top-level JSON body keys.

*** Sort specification (=POST /query=)

#+BEGIN_SRC json
"sort": [{"field": "rating", "order": "desc"}, {"field": "title", "order": "asc"}]
#+END_SRC

- =order= is ="asc"= (default) or ="desc"=.
- The metarecord UUID (ascending) is always implicitly appended as the final sort
  key, so the total order is unambiguous and the keyset cursor can resume
  exactly after the last returned metarecord even when sort values are equal.
- =sort= without =limit= is allowed (sorted full response).
- Sorting by reserved or user fields is allowed alike.

Sort semantics per metarecord for a sort field =F=:

- Metarecords where =F= is unknown (no row) or where every row is =Nothing= sort
  *after* all metarecords with a value, regardless of =order=.
- Multi-map fields: the metarecord's sort key is its smallest value of =F= for
  ="asc"=, its largest for ="desc"=.
- All non-=Nothing= rows of =F= share one value type (see [[*One value type per
  field name]]), so values compare within that single type by its natural order:
  numeric for =Int=/=Float=/=DateTime= (the Unix-ms =value_int=), lexicographic
  for =String=, the *whole path* for =TreeRef= (below), UUID bytes for the
  reference types. No cross-type precedence is needed (it cannot arise within one
  field); only the =Nothing=/unknown-last rule above applies.

**** Sorting a =TreeRef= field

A =TreeRef= sorts on its metarecord's *whole root-relative path*, not on the
=value_name= the string predicates of [[file:spec-query.org][spec-query.org]] compare against: an order
by last component alone interleaves every directory and reads as arbitrary.

Two paths compare *component by component*, each component lexicographically,
and a path that is a prefix of another comes first. So =/photos/2021/a.jpg= <
=/photos/z.jpg= < =/photos-old/a.jpg= — a directory and everything under it stay
together, which comparing the literal path strings would not give (=-= sorts
before =/=). The multi-map rule applies to paths like to any other value: a
metarecord at several locations sorts on its smallest path ascending, its
largest descending.

Implementation note: the index and the oracle both reduce a path to one
*sort key* — its
components joined by =U+0001=, a separator below every character a name may
hold, which makes a plain byte comparison of two keys the component-wise
comparison above. The key is internal (compared, and carried inside opaque
cursors; never displayed). It is never stored: renaming a directory changes the
path of its whole subtree while touching a single field row, so a materialised
key would go stale behind the index's incremental refresh. The in-memory index
rebuilds it from the (resident) tree cache, memoising ancestors. A repository
that serves at all has a resident forest (spec-main "POST /repos/load"), so
there is no second path: a =tree_ref= sort without one is a daemon bug, reported
as such. (The oracle rebuilds the same key with a recursive CTE, which is what
the equivalence battery compares against.)

*** Response envelope

When =limit= is present, the response is wrapped:

#+BEGIN_SRC json
{
  "results": [...],
  "next_cursor": "b64token-or-null"
}
#+END_SRC

=results= has exactly the same item format as the unpaginated response
(UUID strings, or metadata objects when =select= is used). =next_cursor= is
=null= when there are no further results. With =count: true= the envelope
also carries ="total": N= — the full result count of the query at the time
of this page (live data may shift it between pages; clients typically
request it on the first page only, e.g. the metarecord-list status line
"200/5000").

*** Cursor semantics

The cursor is an opaque token (base64-encoded JSON) encoding the sort-key
values and UUID of the last returned metarecord, plus a hash of the =(query,
sort)= pair. Clients must not parse it.

- A cursor is only valid for the same query and sort specification that
  produced it; the daemon verifies the embedded hash and returns =400=
  (=invalid cursor=) on mismatch. Cursors do not expire otherwise.
- Keyset resumption is a =WHERE (sort_key, uuid) > (cursor values)= filter:
  pages are stable against metarecords inserted or deleted in portions of the
  order already consumed. If a metarecord's sort-field value changes between two
  page requests, that metarecord may be returned twice or skipped — this is
  inherent to keyset pagination over live data and is accepted.

** HTTP API

Two layers: *resources* (a single, directly-addressed thing — cheap, idempotent)
and the *=/query/*= set layer* ([[file:spec-query.org][spec-query.org]]). They overlap only on the n=1 case
(a resource path vs a =uuid_in= query), by design. Listing/reading *several*
metarecords is a query (=POST /query=, empty query = all, =uuid_in= = a named
set) — there is no =GET /metarecords= list nor a batch-read endpoint.

#+BEGIN_SRC
POST   /repos/:repo_uuid/metarecords                                   create
GET    /repos/:repo_uuid/metarecords/:uuid                             read one
PUT    /repos/:repo_uuid/metarecords/:uuid                             set whole record
DELETE /repos/:repo_uuid/metarecords/:uuid                             delete one
POST   /repos/:repo_uuid/metarecords/:uuid/fields                      append one row
GET    /repos/:repo_uuid/metarecords/:uuid/fields/:name               read a field's value(s)
PUT    /repos/:repo_uuid/metarecords/:uuid/fields/:name               set a field (replace rows)
DELETE /repos/:repo_uuid/metarecords/:uuid/fields/:name               unset a field
GET    /repos/:repo_uuid/metarecords/:uuid/fields/:name/resolve-tree  TreeRef → path(s)
GET    /repos/:repo_uuid/fields/:id                                    read a field row by id
PATCH  /repos/:repo_uuid/fields/:id                                    rename and/or revalue a row
DELETE /repos/:repo_uuid/fields/:id                                    delete a row by id
POST   /repos/:repo_uuid/retype                                        retype a field repo-wide
GET    /repos/:repo_uuid/fields[?type=<value_type>]                    distinct field names + types
GET    /repos/:repo_uuid/tree/roots[?field=<field>]                    TreeRef forest roots (uuid + name)
POST   /repos/:repo_uuid/tree/resolve-path                            path → node uuid (inverse of resolve-tree)
#+END_SRC

*** =POST /repos/:repo_uuid/metarecords=

Creates a metadata metarecord. Body ={"fields": [{"name", "value"}, ...], "force"?}=.
For =TreeRef= fields the client supplies =(parent_uuid, name)= directly; the
daemon validates the =parent_uuid= (null = a root, else an existing metarecord
carrying that =TreeRef= name). The filesystem root (=value_uuid = x'00...00'=,
=value_name = ""=) is created at =repo init= and is unique per field name. A
metarecord may carry several =mfr_path= rows (multi-map = several locations).
Response: the full metadata object (=id= on each field).

*** =POST /repos/:repo_uuid/metarecords/bulk=

Creates *many* metarecords, each at its own optional caller-supplied UUID, in a
*single revision*. The one-at-a-time form costs a revision — and an =fsync= —
per metarecord, which is the difference between restoring a directory from the
trash-bin and giving up on it.

#+BEGIN_SRC json
{"metarecords": [{"uuid": "<32hex>", "fields": [{"name", "value"}, ...]}, ...],
 "force": false,
 "skip_existing": false}
#+END_SRC

- =uuid= is optional per metarecord: omitted, the daemon draws one, exactly as
  the single-record form does. The endpoint is a general bulk create.
- =force= applies to the whole batch (reserved-field override, as on the
  single-record form).
- =skip_existing= makes a UUID that is already taken a *skip* instead of a
  refusal. Without it, a caller that cannot know in advance whether a metarecord
  survives — a subtree restore meeting a node the watcher has re-tracked — would
  have to precede every batch with a filtering query.

Response: ={"created": N, "skipped": M, "uuids": [...]}=, the UUIDs in the order
given (which is how a caller learns the ones the daemon drew).

*Nothing carries a version.* A version is a hash of the metarecord's content
([[*Version]]), so recreating a metarecord at its UUID with its fields gives it
back exactly the version it had. Field row =id=s are freshly allocated — they are
storage identity, not content.

*All-or-nothing*, like every other batch writer: one revision, one transaction,
and the first refusal — a taken UUID (=409=), a reserved field without =force=,
a schema violation (=400= with the =violations= array, each naming its
=metarecord_uuid=) — rolls the whole batch back. A half-applied restore would be
worse than none.

*Order is significant, and it is the caller's to get right.* Metarecords are
applied in the order given, and the forest refuses a =TreeRef= whose parent has
no row yet ([[*Referential integrity of a forest]]), so a subtree must arrive
parent-first.

*Limits.* At most 1000 metarecords per call; beyond that the call is refused with
=400= naming the limit, before any work. The request body is capped
independently (2 MiB), so a batch of metarecords carrying many fields reaches
that first — callers page, as the trash-bin's own subtree capture already does.

*** =GET /repos/:repo_uuid/metarecords/:uuid=

Returns one metarecord with all its fields. Each field carries its DB-row =id=
(used by the by-id endpoints).
#+BEGIN_SRC json
{ "uuid": "...", "version": 3,
  "fields": [ {"id": 42, "name": "rating", "value": {"type": "int", "value": 5}} ] }
#+END_SRC

*** =PUT /repos/:repo_uuid/metarecords/:uuid=

*Whole-record set*: replaces the entire field set, keeping the UUID, as one
=SetRecord= operation (=before= = all old fields, =after= = the given set).
Literal overwrite — fields not listed are dropped, reserved =mfr_*=/=mf_*=
included (so a careless set can untrack a file; round-trip =GET= to preserve).
Body ={"fields": [...], "force"?}= → updated full object.

*** =DELETE /repos/:repo_uuid/metarecords/:uuid=

Deletes the metarecord and all its fields (one =DeleteRecord= op). =204=.

*** =POST /repos/:repo_uuid/metarecords/:uuid/fields=

Appends one row without touching existing rows of that name (multi-map append).
Body ={"name", "value", "force"?}= → updated full object. Protected name without
=force= → =400=. Appending a value the metarecord already holds under that name
is a no-op (see [[*No duplicate rows]]): the record comes back unchanged, same
=version=, and nothing is logged.

*** =GET|PUT|DELETE /repos/:repo_uuid/metarecords/:uuid/fields/:name=

A field of one metarecord, addressed by name (the direct analog of the
=/query/fields/{...}= set operations):
- =GET= → the field's value(s): ={"name": "tag", "values": [<Value>, ...]}=
  (empty list when the field is unknown).
- =PUT= → *set*: replaces all rows of =name= (one =SetField= op). Body =value=
  (one row) or =values= (a list, multi-map) — exactly one.
- =DELETE= → *unset*: removes every row of =name= (the field becomes unknown).
Protected names need =force= (in the body for PUT/DELETE). Updated full object
(PUT) / =204= (DELETE).

*** Conditional writes (=expected_version=)

The single-record write endpoints (whole-record =PUT=, metarecord =DELETE=,
field =POST=, field =PUT=/=DELETE= by name) accept an optional
=expected_version= query parameter: the write is rejected with =409=
(nothing written, no revision) when the metarecord's current =version=
differs from the given value. This
is the optimistic-concurrency primitive used by cross-repo sync propagation
([[file:spec-sync.org][spec-sync.org]]) so a concurrent user write is never silently overwritten;
ordinary clients may use it for the same purpose.

Because the version is a content hash ([[*Version]]), this guard is
*content-based*: a write conditioned on =H= succeeds even if the metarecord went
from =H= to some other state and back to =H= in between. That is the intended
reading — the precondition asks "is the state I read still the state I am
writing against", and a byte-identical state is that state. Nothing is silently
overwritten, which is what the guard exists to prevent.

*** =GET /repos/:repo_uuid/metarecords/:uuid/fields/:name/resolve-tree=

Resolves a =TreeRef= field of this metarecord to its root-relative path(s).
Response ={"paths": ["<path>", ...]}= (empty when the field is absent or not a
=TreeRef=). The set form is =POST /query/fields/resolve-tree= ([[file:spec-query.org][spec-query.org]]).

*** =GET|PATCH|DELETE /repos/:repo_uuid/fields/:id=

A single field *row* by its repository-unique DB id (no metarecord UUID in the
path — the id alone addresses it). =404= if the id is unknown.
- =GET= → the row: ={"id", "name", "value"}=.
- =PATCH= → change its *name and/or value* in place, keeping the id; the value
  type is validated against the (target) field name. Body ={"name"?, "value"?, "force"?}=.
  =400= if the result would duplicate another row of the same name on that
  metarecord (see [[*No duplicate rows]]).
- =DELETE= → remove the row. Body ={"force"?}=, =204=.

*** =POST /repos/:repo_uuid/retype=

Converts a field's value type repository-wide to any type
(=string|int|float|bool|datetime|ref|tree_ref|externalref|refbase=)
— see [[*Changing a field's type (retype)]]. Body ={"name": "<field>", "to": "<type>"}= →
={"converted": <n>, "fallback_count": <n>, "fallback_uuids": [...]}=. Reserved
=mfr_*=/=mf_*= names are rejected (the system owns their types).

*** =GET /repos/:repo_uuid/fields=

Lists the distinct field names present on the repository's metarecords, each
with its value type, ordered by name:

#+BEGIN_SRC json
[{"name": "category", "type": "tree_ref"}, {"name": "tag", "type": "ref"}, ...]
#+END_SRC

Since a field name has a single non-=Nothing= value type repository-wide ([[*One value type per field name]]),
each name appears at most once. =Nothing= rows are excluded (an explicit
absence is not a usable value). An optional =?type=<value_type>= query
parameter restricts the result to one value type (=tree_ref=, =ref=, =string=,
…). The isolation matches a query's universe — every metarecord of this
repository's database (which, one owner per metarecord, is the same set as
"owned exclusively by it"; see the roadmap item on link-awareness). The GUI uses
it to populate field pickers (e.g.
the TreeRef-field and Ref-field selectors of the tree-explorer panel, see
[[file:spec-gui.org][spec-gui.org]]).

The catalog is served from the daemon's resident query index, and *never waits
for a write*. Bringing that index up to HEAD needs the repository's database
connection, which a long write (a reconcile, a large watcher flush) holds for its
whole transaction; the endpoint therefore /tries/ the connection and, when a write
holds it, answers from the index as it stands. Nothing is lost by that: an
in-flight write is uncommitted, so the resident catalog already is the committed
state — and the client sees the write's new names when it re-warms the catalog on
the change the log feed reports. Waiting instead would trade a millisecond for
the length of someone else's reconcile.

*** =GET /repos/:repo_uuid/tree/roots=

Lists the *forest roots* of a TreeRef field — the nodes whose direct parent is
the root sentinel (no parent). =?field=<field>= selects the forest (defaults to
=mfr_path=). Response =[{"uuid": "<hex>", "name": "<name>"}, ...]= ordered by
name. This is the top-down entry point a query cannot express: =Follows= with
the empty path resolves to the sentinel's *children*, and only when a root is
literally named =""= (as =mfr_path='s repo-root metarecord is); the roots
themselves — e.g. the top-level nodes of a tag tree — are otherwise
unreachable. The tree-explorer panel ([[file:spec-gui.org][spec-gui.org]]) starts here, then descends
with =Follows{field, target: \{uuid_in: [node]\}\}=.

*** =GET /repos/:repo_uuid/tree/children=

The step down from a node: the *direct children* of one node of a TreeRef
forest. =?field=<field>= selects the forest, =?uuid=<hex>= the parent node (both
required; the root sentinel's children are [[*=GET /repos/:repo_uuid/tree/roots=][the roots]]). Response
=[{"uuid": "<hex>", "name": "<name>"}, ...]= ordered by name — the same shape as
=tree/roots=, so a caller descending a tree reads one format.

It is served from the resident tree cache (=TreeCache::children_of=), which is
why it exists next to the query layer rather than inside it: "the children of
this node" is also =field:parent = "<path>"= (spec-query "Field aspects"), but
that form resolves a *path* and returns uuids to be assembled, where a tree
walker already holds the parent's uuid and wants the names it descends by. The
file-manager panel's tracked listing is one call of this per directory opened.

*** =POST /repos/:repo_uuid/tree/resolve-path=

The inverse of =resolve-tree=: resolves a repo-root-relative path (components
split on =/=, in the [[file:spec-file-tracking.org][Path string format]] — the filesystem forest is
leading-=/=-rooted, so its first component is the empty root name; a named-root
forest has no leading =/=) in a TreeRef forest to the uuid of the node at that
path. Body ={"field"?: <field> (default =mfr_path=), "path": "/music/jazz",
"form"?: "verbatim" | "escaped"}=; response ={"uuid": "<hex>" | null}= (null = no
such node). Used to *set* a TreeRef value from a path: resolve the parent path to
a uuid, then post =(parent, name)=. One in-memory round-trip through the tree
cache.

A node's name is never empty, so an *empty component after the first* can only
come from a redundant slash and is dropped: ="/"= names the filesystem forest's
root (whose canonical form, the one =resolve-tree= returns, is the empty string),
="/music/"= and ="/music//jazz"= name the same nodes as their canonical
spellings. This is what lets a UI spell the repository root the readable way — a
bare ="/"= — in a query it hands back to the daemon.

Sibling names are unique, so at most one node matches — *per reading*. A
component spelling the escaped form of an undecodable byte can name two
different files at once (one really called =%E9.txt=, one holding the byte
=0xE9=); =form= says which is meant, and without it such a path answers =null=
rather than a guess. See [[*Tree names]].

* CLI

Metarecord manipulation commands. Global conventions (binary name, exit codes)
are defined in [[file:spec-main.org][spec-main.org]].

*** Global selection flags

The repository and daemon are chosen *once*, by flags that precede the command —
never per-command:

| Flag           | Selects                                                     |
|----------------+------------------------------------------------------------|
| =-n <name>=    | the loaded repository by its (unique) name                 |
| =-u <uuid>=    | the loaded repository by UUID                              |
| =-p <port>=    | the daemon TCP port on =127.0.0.1= (default =7523=)        |

=-n= and =-u= are mutually exclusive. The daemon only listens on loopback, so a
port is all that is needed to address it (no full URL). Environment fallbacks:
=METAFOLDER_REPO= (UUID), =METAFOLDER_REPO_NAME= (name), =METAFOLDER_DAEMON_PORT=.
Daemon-wide commands (=mf repo=, =mf task=) ignore =-n=/=-u=.

Repository *names are unique among loaded repositories*: the daemon rejects
loading a repo whose name collides with one already loaded, so =-n= always
resolves to a single UUID.

** Field specification syntax

Commands that write fields take a /field spec/ argument of the form
=name:type[=value]=:

| Type          | Syntax                                  | Example                                           |
|---------------+-----------------------------------------+---------------------------------------------------|
| =nothing=     | =name:nothing= (no value)               | =rating:nothing=                                  |
| =string=      | =name:string=text= (quotes optional)    | =genre:string=jazz=, =genre:string="hard bop"=    |
| =int=         | =name:int=N=                            | =rating:int=5=                                    |
| =float=       | =name:float=X=                          | =score:float=3.14=                                |
| =bool=        | =name:bool=true|false=                  | =seen:bool=true=                                  |
| =datetime=    | =name:datetime=ISO-8601=                | =added:datetime=2024-01-01T12:00:00Z=             |
| =ref=         | =name:ref=<metarecord_uuid>=                 | =author:ref=8f3a...=                              |
| =tree_ref=    | =name:tree_ref=<parent_uuid>/<name>=; empty parent for a root: =name:tree_ref=/<name>= | =parent:tree_ref=8f3a.../félins= |
| =refbase=     | =name:refbase=<repo_uuid>=              | =origin:refbase=47ab...=                          |
| =externalref= | =name:externalref=<repo_uuid>:<metarecord_uuid>= | =link_a:externalref=47ab...:8f3a...=          |

** Selector options

=mf metarecord= picks its target metarecord(s) with a *selector option that
precedes the verb* (so the selector and verb don't compete for the same
positional, and one selector serves every verb including =field=):

| Option        | Selects                                                       |
|---------------+--------------------------------------------------------------|
| =-i <uuid>=   | one metarecord by UUID (no daemon query round-trip)          |
| =-q "<DSL>"=  | the metarecords matching a DSL predicate ([[file:spec-query.org][spec-query.org]])         |
| =-s=          | with =-q=: treat it as simplified-language text, expand first |
| (none)        | all metarecords                                              |

=-q= and =-i= are mutually exclusive; =-s= requires =-q=. =-s= expansion is a
pure client-side transformation (the shared grammar in =core=), never a daemon
round-trip.

*The typed flag decides, never the shape of its text.* A bare 32-hex UUID is
valid DSL (a =UuidIn= atom — see [[file:spec-query.org][spec-query.org]]), so
=-q <uuid>= is an ordinary query and behaves like every other =-q=, while
=-i <uuid>= keeps the resource-layer behaviour. The two differ only in output
shape (=get=) and in what =field get --resolve= accepts; every other verb
compiles both to the same =uuid_in= request. Where a target is instead a
*positional* =<query|uuid>= argument (=mf path=, =mf tag= targets…), no flag was
typed and the shape decides, as before. Example: =mf -n music metarecord -q 'mfr_path ->* "/docs"' field set reviewed:bool=true=.

** Verb pattern

The repo-scoped data commands form a tree (=metarecord=, =field=) whose verbs
follow one pattern at every level:

- =get= reads; =set= replaces *everything at that level*; =add= creates a new
  element (with a fresh id/UUID); =remove= takes one back out *by value*;
  =delete= destroys one addressed *by its id* (or UUID).

So =get= is the inverse of =set=, and =add= the inverse of =remove= (by value)
and of =delete= (by id). The two removal verbs are not synonyms: =remove= names
a value inside a multi-map and leaves the other rows of that name alone, while
=delete= destroys a thing that has an id — a metarecord (=mf metarecord
delete=), a field row (=mf field delete <id>=). Keeping them apart is what stops
"take this tag off the record" from reading like "destroy the record"; the same
four verbs name the daemon's set layer (=POST
/query/fields/{set,add,remove,unset}=) and the GUI's =metarecord:field= /
=metarecord:bulk= operations, so one word means one thing in all three.

=set= at the metarecord level replaces all of its fields; at the field-name
level, all rows of that name; at the row level, the one row. =add= lives only
where new ids are born (a metarecord, or a row under a field name), so the by-id
=field= group has no =add=.

** Daemon- and repo-management groups

*** =mf repo {list|init|load|unload}=

Daemon-wide; take no =-n=/=-u= selector. =list= (the default for =mf repo=)
pretty-prints the loaded repositories; =init <path>= / =load <path>= print the
repo UUID; =unload= releases the repo selected by =-n=/=-u=.

=init= additionally applies the =default= ignore preset to the new root (see
=mf ignore= and spec-file-tracking "Ignore presets"): the daemon creates a root
with an empty =mf_ignore= set, then the shared =core::repo_init= orchestration
writes the =default= preset's patterns. =--ignore <names>= selects a different
preset set (comma/space separated); =--no-ignore= leaves the root's ignore set
empty. A missing =ignore-presets.toml= makes =init= fail (never a silent
fallback), unless =--no-ignore= is given.

*** =mf task {list|show}=

Background tasks (spec-tasks). =list= (default) lists tasks (=--all= across
repos); =show <id>= shows one (=--stop= cancels it).

*** =mf log {list|rollback|prune}=

Repository history (spec-event-log). =list= (default) is the former flat =mf
log=; =rollback= and =prune= are unchanged.

*** =mf retype <name> <type>=

Converts a field's value type repository-wide (=string|int|float|bool|datetime=).
Kept at the top level deliberately: it is a *name-scoped, whole-repository*
migration, not a selection- or id-scoped edit, so it belongs to none of the
get/set/add/delete groups.

** =mf metarecord [selector] <verb>=

The verb follows the selector options above. =add= takes no selector; =set=
needs =-i=; =delete= and =field= need a selector; =get= defaults to all.

*** =mf metarecord [-i|-q] get=

Reads metarecords; subsumes the former =list=, =query= and =get=:
- with =-i <uuid>=, prints that metarecord's full JSON object (fields incl. ids);
- with =-q= or no selector (match-all), prints the matching UUIDs, one per line
  (internal pagination over =next_cursor=) — including =-q <uuid>=, a query like
  any other (see the flag-decides rule under [[*Selector options]]).

The selector may also be given as =--eq name[:type]=value= (repeatable, AND-ed):
a *safe* exact-match query built without interpolating anything into the query
DSL — the value is escaped, so a name with quotes cannot inject. Bare
=name=value= defaults to a string; mutually exclusive with =-q=/=-i=. This is the
selector the tagging scripts use instead of hand-quoting =title = "$X"=.

=--select=, =--sort=, =--limit=, =--values= override the default output shape.
=--count= replaces it: it prints *how many* metarecords the selector matches, on
one line, and no UUID. It is one round-trip whatever the size of the scope — the
daemon counts through the bitmap index (=count: true= on =POST /query=, O(1)) —
so a walk that needs "how many are left" after every answer does not pay for the
list it is not printing. Query selectors only: =-i= names a single metarecord,
so =--count= with it is a usage error (exit 2), as it is with the flags that
shape rows (=--select=, =--values=, =--tsv=, =--sort=, =--limit=).
=--select a,b,c --tsv= prints *one tab-separated row per metarecord* — the first
value of each selected field, in order, empty for an absent field — the compact
dump the scripts consume (e.g. the tag vocabulary).

=--resolve-tree <field>= is the *bulk* form of =mf path=: it resolves the given
=TreeRef= field of every selected metarecord to its root-relative path(s), one
per line (sorted, deduplicated), in a single =POST /query/fields/resolve-tree=
round-trip (paths in the endpoint form — leading-=/=-rooted for the filesystem
forest, so a line pastes straight into an =mfr_path -> "…"= query; the repo root
resolves to an empty line). It needs a selector (=-q= or =-i=) and is exclusive
with =--select=/=--values=/=--sort=. This is what lets a GUI script offer
folder/file completions (=mfr_path=) without a per-record loop.

With =--tsv= each line is =uuid<TAB>path= instead (one row per path, sorted by
path, so both forms list in the same order). The endpoint answers with a
uuid→paths map and the bare-path form throws the keys away, which is all a
completion list needs; a script that has to *act* on the records — walk them,
tag them — needs to know which record a path belongs to, and would otherwise
fall back to one =mf path= call per record. This is the one place =--tsv= does
not require =--select=; clap cannot express a conditional =requires=, so the
rule is checked in the command and is still a usage error (exit 2).

*** =mf metarecord add <spec> [<spec> ...]=

Creates a metarecord with the given fields and prints its UUID (the former =mf
create=). Takes *no selector* (it makes a new metarecord). The inverse of
=mf metarecord -i <uuid> delete=.

| Flag      | Description                                                |
|-----------+------------------------------------------------------------|
| =--force= | Required to write =mfr_*= fields (see [[*Reserved fields]]) |

*** =mf metarecord -i <uuid> set <spec> [<spec> ...]=

Replaces the *entire* field set of the selected metarecord, keeping its UUID —
the inverse of =mf metarecord -i <uuid> get=, equivalent to a delete + create at
fixed UUID. *Literal overwrite*: any field not listed is removed, including
reserved =mfr_*=/=mf_*= ones, so a careless set can untrack a file (round-trip
the output of =get= to preserve everything). Recorded as *one* =SetRecord=
operation. *Needs =-i=* (a single metarecord) and *requires =-f=/=--force= to run
at all* — a deliberate guard so this rare, scripting-oriented overwrite is not
confused with =field set= (one field).

| Flag          | Description                                                |
|---------------+------------------------------------------------------------|
| =-f=/=--force= | Mandatory: confirms the full-record overwrite             |

*** =mf metarecord [-i|-q] delete=

Deletes the selected metarecords (metadata and all fields); prints the count.
Requires a selector. Confirms for a =-q= selector unless =--force= is passed. A
query goes through the atomic =POST /repos/:repo/query/delete= (one revision); the
confirmation count comes from a =count=+=limit= query.

*** =mf metarecord [-i|-q] field <verb> ...=

Field operations scoped to the selected metarecord(s) (the selector is required).
=get=/=unset= take a bare field =name=; =set=/=add=/=delete= take field specs:

- =get <name> [--resolve <field>]= — prints the field's value(s). With
  =--resolve=, treats each value as a =Ref= and prints the referenced
  metarecords' =<field>= instead — the ref→field join resolved in *one*
  round-trip (a =uuid_in= query), e.g. a record's tag paths from its =tag= refs.
  When =<field>= is a =TreeRef= its resolved path is printed (a second
  round-trip), not the raw =(parent, name)=. =--resolve= requires =-i=;
- =set <spec> [<spec> ...]= — replaces *all* rows of the field with the given
  value set (one or several — multi-map; the specs must share one name), in *one*
  log operation per metarecord;
- =add <spec>= — appends one row (inverse of =remove=);
- =remove <spec>= — removes the row(s) equal to the spec's =(name, value)=
  (inverse of =add=); other rows of the same name, and non-matching metarecords,
  are kept;
- =unset <name>= — removes the field *entirely* (all rows → /unknown/), distinct
  from =set name:nothing= (an explicit =Nothing= row).

A UUID selector targets that one metarecord; a predicate applies the write across
every match in one server-side transaction (one revision, one operation per
affected metarecord). =-f=/=--force= is required for reserved =mfr_*= fields.

** =mf tag [-i|-q|--eq] <verb> <path>= (hierarchical tags)

Applies a *hierarchical tag convention* to the selected metarecord(s), owning
the model the =gui-tag-*= scripts used to re-implement by hand. A *tag entry* is
a metarecord =mf_schema = <entry_type>= (=tag=) whose position in the tag hierarchy is
a =TreeRef= field =<path_field>= (=path=) — its resolved path (=musique/jazz=) is
the tag's identity, and a nested path is a chain of tag-entry nodes (the CLI
creates missing ancestors). Records reference entries through the multi-map =Ref=
fields =<positive>=/=<negative>=/=<mixed>= (=tag=/=negative_tag=/=mixed_tag=). All
field names are configurable via the CLI config =[tag]= table (defaults reproduce
this convention). A tag is selected in the DSL by its path with the exact-node
form =path = "musique/jazz"= (see [[file:spec-query.org][spec-query.org]]).

Verbs (the selector precedes the verb, as for =mf metarecord=):
- =add <path>= — the record(s) *have* the tag: idempotently add the positive
  ref, then drop the more general *ancestor* tags, and — when =<path>= is
  =exclusive= (own flag, or its parent is a =partition=) — drop its *siblings*;
- =deny <path>= — the record(s) do *not*: add the negative ref, drop the more
  specific *descendant* negatives it subsumes;
- =mixed <path>= — mark the folder(s) mixed w.r.t. the tag (no subsumption);
- =remove <path>= — drop the positive ref (undo of =add=);
- =list= — the vocabulary as TSV =path<TAB>partition<TAB>exclusive= (0/1),
  path-sorted — the universe format the tagging flow consumes; takes no selector.

=add=/=deny=/=mixed= auto-create the tag entry when the vocabulary lacks it. The
write is applied across the whole selector set through the batch field endpoints
(=POST …/query/fields/{add,remove}=), one call per set — no per-record loop.
So a whole subtree is tagged with =mf tag -q 'mfr_path ->* "/dir"' add <path>=.

Their idempotence is the daemon's: appending a ref a record already carries is a
no-op (see [[*No duplicate rows]]), so the ref is appended once and the row that
already held it is left untouched, id included — no remove-then-append. The
number printed is therefore how many records actually *gained* the ref: re-running
the same =add= prints =0=, and the subsumption removals that follow are unaffected.

** =mf ignore <verb> <presets>= (named ignore presets)

Applies named ignore presets (spec-file-tracking "Ignore presets") to a target
directory metarecord's =mf_ignore= set. The target is chosen by =-d <path>= (a
repo-root-relative directory path, resolved to its metarecord through the
exact-path idiom, as =mf trash -f= does), defaulting to the *root* when omitted.

- =add <presets>= — expand the presets (client-side) and *append* their patterns
  to the target's =mf_ignore= set (deduplicating rows already present);
- =remove <presets>= — remove exactly those patterns from the target's set;
- =set <presets>= — *replace* the target's whole =mf_ignore= set with the
  expanded patterns (drops all current rows first); =set= with no preset clears
  the set. This is the =add=⁻¹-style overwrite, mirroring =mf metarecord set=;
- =list= — the group default (=mf ignore= ≡ =mf ignore list=): lists the
  available presets with their descriptions; with =-d= it also prints the
  patterns currently active on the target.

=<presets>= is one or more preset names (comma or space separated), e.g.
=mf -n music ignore add rust-build,node -d /src=. A missing =ignore-presets.toml=
is a usage error (exit 2) pointing at =metafolder-sync-config= — never a silent
fallback. Expansion and preset loading live in =core= (=core::ignore_presets= /
=core::ignore=), shared with the GUI; the daemon only ever receives the final
=mf_ignore= strings.

** =mf field <verb> <id>= (direct row access)

Operates on a single field row by its DB id (as shown by =mf metarecord get=):

- =list [--type <value_type>]= — lists the repository's *distinct field names*
  with their value type (=GET …/fields=), one =name<TAB>type= per line, ordered
  by name; =--type= restricts to one value type (=tree_ref=, =ref=, =string=,
  …). This is the group's default verb (=mf field= ≡ =mf field list=). Unlike
  the other field verbs it is repo-wide and takes no id;
- =get <id>= — prints the row (JSON);
- =set <id> <spec>= — sets the row to the field spec's name and value in place,
  *keeping its id*; a differing name renames the row, and the value type is
  validated against the (target) field name's established type. Refused (=400=)
  when the result would duplicate another row of that name on the same
  metarecord (see [[*No duplicate rows]]);
- =delete <id>= — removes the row.

There is no =mf field add=: new rows are born at the name level (=mf metarecord
field … add=). =-f=/=--force= is required for reserved =mfr_*= fields.

** Log granularity

Every command maps to *one log operation per metarecord it changes* — the unit
the log shows and rollback reverses:

| Command                       | Operation(s)                                  |
|-------------------------------+-----------------------------------------------|
| =metarecord add= / =delete=   | one =CreateRecord= / =DeleteRecord=           |
| =metarecord set=              | one =SetRecord= (all fields before→after)     |
| =field set <name>= (n values) | one =SetField= (all rows of the name)         |
| =field add=                   | one =AppendField= (none if the value is already there) |
| =field remove= (by value)     | one =DeleteField= (the removed rows)          |
| =field unset=                 | one =DeleteField= (all rows of the name)      |
| =field set <id>= / =delete=   | one row-scoped op (keeps the row id)          |

A query-scoped write is still *one revision* containing one such operation per
affected metarecord.

* Open questions
