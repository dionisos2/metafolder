{
  "name": "metafolder-gui-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.json --fail-on-warnings"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6",
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^3.2.7",
    "jsdom": "^26",
    "svelte": "^5",
    "svelte-check": "^4.7.2",
    "typescript": "^5",
    "vite": "^7",
    "vitest": "^3"
  }
}
