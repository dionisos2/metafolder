// Stub: the POC CLI was removed together with the old data model (see git
// history). The v1 CLI (`mf`) will be implemented once the v1 daemon is done
// (roadmap: "first version of the Rust commands").
fn main() {
    eprintln!("error: the metafolder CLI is not implemented yet (daemon v1 in progress)");
    std::process::exit(2);
}
